Use OpenAPI to define what API the frontend can consume

Use integration testing to validate all functionalities work as needed. Use repository pattern (with interfaces) to fake a server, to take that out of the equation
